name: Docker Image CI

on:
  push:
    branches: [ "temp" ]
  pull_request:
    branches: [ "temp" ]

jobs:

  build:

    runs-on: ubuntu-22.04

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag milestone3:$(date +%s)
